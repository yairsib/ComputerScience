package Classes.Q1;

import java.util.Random;

public class Main {
    public static void main(String[] args) {
        Clock clock = new Clock();
        clock.minutes = 17;
        clock.seconds = 45;
        clock.hours = 2;
        clock.ampm();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.tick();
        clock.print();
    }


}